import React from 'react';
import { CustomFilters } from '@/components'

const CustomFiltersPage = () => {
  return (
    <div>
      <CustomFilters />
    </div>
  );
};

export default CustomFiltersPage;
